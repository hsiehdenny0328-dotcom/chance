# 標題左側加入高雄市立楠梓高級中學校徽與名稱實作計畫

本計畫旨在回應使用者的請求，將「高雄市立楠梓高級中學」的校徽（Logo）以及學校名稱文字，優雅且高質感地打在網頁主標題的左側。這樣一來，不僅能夠將專案客製化為該校的專屬互動教學網頁，同時也能保持極佳的視覺美感與響應式排版（RWD）效果。

## 使用者審查請求

> [!IMPORTANT]
> 為了提供最精美的視覺體驗，我們規劃了以下設計細節，請您確認是否符合您的期望：
> 1. **校徽與文字整合**：我們將原本標題左側的 3D Logo 替換為您提供的「高雄市立楠梓高級中學」圓形藍色校徽，並在標題上方/左方優雅地打上「高雄市立楠梓高級中學」字樣，形成精緻的學校品牌標籤。
> 2. **圓形校徽適配**：由於楠梓高中的校徽為圓形設計，我們將原本的 `.hero-logo` 樣式從方圓角（`border-radius: 18px`）調整為完美的圓形（`border-radius: 50%`），使邊框、毛玻璃效果與校徽圖形完美契合。
> 3. **響應式適配**：在行動裝置（螢幕寬度小於 600px）上，我們將自動將校徽、學校名稱與主標題切換為垂直置中排版，確保在手機上也能完美顯示。

## 開放性問題

> [!NOTE]
> 暫無影響開發的重大開放性問題。我們將預設採用「上方小字學校名 + 下方大字主標題」的精緻堆疊排版，這在現代學術與教學網站中非常流行且具質感。如果您有其他排版想法，歡迎在審查時告訴我們！

---

## 預期變更

### 1. 專案基礎設定

將 `.agent/` 資料夾排除於 Git 版本控制之外，避免自動產生的工具檔案影響您的專案儲存庫。

#### [NEW] [.gitignore](file:///c:/Users/USER/Downloads/chance/.gitignore)
- 建立 `.gitignore` 檔案並寫入 `.agent`，確保開發代理程式的暫存檔不會被提交。

#### [NEW] [nzsh_logo.png](file:///c:/Users/USER/Downloads/chance/nzsh_logo.png)
- 將您上傳至系統的校徽檔案（`media__1780300693417.png`）複製至專案根目錄下，重新命名為更具識別性的 `nzsh_logo.png`。

---

### 2. 網頁結構與樣式修改

調整標題的 HTML 架構與 CSS 排版，完美呈現校徽與「高雄市立楠梓高級中學」字樣。

#### [MODIFY] [index.html](file:///c:/Users/USER/Downloads/chance/index.html)
- 替換原先的 `probability_logo.png` 為全新的 `nzsh_logo.png`。
- 修改標題結構，在 `<h1>` 的 `.hero-title` 容器內引入 `.title-text-group`，將「高雄市立楠梓高級中學」與「機率與統計互動教學」以語意化的 `<span>` 元素進行包覆。
- **修改代碼結構預覽**：
  ```html
  <h1 class="hero-title">
    <img src="nzsh_logo.png" alt="高雄市立楠梓高級中學 校徽" class="hero-logo" />
    <div class="title-text-group">
      <span class="school-name">高雄市立楠梓高級中學</span>
      <span class="main-title">機率與統計互動教學</span>
    </div>
  </h1>
  ```

#### [MODIFY] [style.css](file:///c:/Users/USER/Downloads/chance/style.css)
- **校徽樣式優化**：調整 `.hero-logo` 的 `border-radius` 為 `50%`，並針對圓形校徽微調毛玻璃與邊框高光，讓它看起來更有立體水晶質感。
- **標題文字排版**：
  - 新增 `.title-text-group` 樣式，使用 `flex-direction: column` 與 `align-items: flex-start`，使學校名稱與主標題優雅地垂直排列。
  - 新增 `.school-name` 樣式：設定為稍小的字體大小（例如 `1.1rem`）、稍微寬鬆的字距（`letter-spacing`），並賦予半透明的白色（如 `rgba(255, 255, 255, 0.85)`），使其呈現精緻的附屬學校名稱質感。
  - 新增 `.main-title` 樣式：保持原本醒目、大氣的主標題字體大小。
- **響應式排版修正 (RWD)**：
  - 在 `@media (max-width: 600px)` 內，將 `.title-text-group` 的對齊方式微調為垂直居中，並配合 `.hero-title` 的垂直排列，確保在窄螢幕上的美觀度。

---

## 驗證計畫

### 模擬與人工驗證
1. **靜態檢查**：確保 HTML 檔案符合 UTF-8 編碼，無任何亂碼，且所有註解均以正體中文編寫，並包含詳細的教學註解以利學生學習。
2. **視覺排版檢查**：在瀏覽器中開啟 `index.html`，檢查：
   - 楠梓高中校徽是否正確顯示在左側。
   - 校徽的滑鼠移入懸停動畫（回彈縮放與旋轉）是否依然滑順且完美契合圓形。
   - 「高雄市立楠梓高級中學」文字是否正確呈現在標題左側上方，字體精緻且易讀。
3. **響應式測試**：縮放瀏覽器視窗寬度至 600px 以下，確認排版會流暢地轉換為行動裝置專用的垂直居中排列，無任何文字重疊或版面破損。
